{% extends 'base.html.twig' %}

{% block body %}
<h2>Inscription</h2>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">S'inscrire</button>
{{ form_end(form) }}
{% endblock %}
